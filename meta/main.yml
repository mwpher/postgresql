# file: postgresql/meta/main.yml

galaxy_info:
  author: pjan vandaele
  company: ANXS
  description: "Install and configure PostgreSQL, dependencies, extensions, databases and users on FreeBSD."
  min_ansible_version: 1.4
  license: MIT
  platforms:
  - name: FreeBSD
    versions:
    - all
  categories:
  - database
  - database:sql

dependencies: []
